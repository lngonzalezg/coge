// Build stamp shown over the logo on non-production hosts.
// Overwritten at image-build time by the Dockerfile (build datetime + COGE_REF).
// Empty here so a plain source checkout falls back to " Development".
window.COGE_BUILD = '';
